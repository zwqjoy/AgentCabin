# HTML/SVG 信息图与交互演示

## 目录

- [专项加载](#专项加载)
- [适用范围](#适用范围)
- [静态与交互选择](#静态与交互选择)
- [常用信息图形态](#常用信息图形态)
- [素材形态](#素材形态)
- [输出约束](#输出约束)
- [内容校验](#内容校验)
- [静态信息图要求](#静态信息图要求)
- [交互要求](#交互要求)
- [降级](#降级)

本文件是普通静态 HTML/SVG 和基础交互任务的完整运行契约。常规流程、机制、架构、关系、时间线、状态机和比较图读完本文件即可生成。

## 专项加载

- 高密度排版、专用库或特殊视觉设计：读 `renderer-trigger-design.md`。
- 明确移动端且存在复杂分栏、长文本或密集控件：读 `renderer-output-mobile.md`。
- 使用脚本、动画或动态公式：读 `renderer-stability-math.md`。
- 使用拖拽、播放、几何约束、DOM/ECharts 联动：再读 `renderer-interaction-geometry.md`。

普通精确数据图仍优先使用原生 ECharts option。

## 适用范围

用于确定性表达：

- 流程、机制、因果和反馈回路。
- 系统架构、层级、组件关系和数据流。
- 人物、组织、角色和知识关系。
- 时间线、阶段演进、生命周期和状态机。
- 对比、选型、分类和层级。
- 算法步骤、证明过程、参数变化、物理过程和动态几何。
- 基于用户原图的点击讲解、逐步高亮或部件切换。

不用于海报、头像、壁纸、写实图片或艺术插画创作。结构无法可靠确定时回退文字。

## 静态与交互选择

### `static`：默认

以下内容优先静态 HTML/SVG：

- 阅读方向固定的流程和时间线。
- 节点与连线可一次呈现的关系和架构。
- 不需要改变参数的机制、因果和比较。
- 初始画面即可完整理解的状态结构。

### `interactive`：必须有理解增益

只有以下情况选择交互：

- 点击节点会显示不同但必要的说明。
- 拖动参数会改变曲线、轨迹、几何或状态。
- 播放步骤能揭示算法、证明或物理过程。
- 切换视图能帮助比较不同模型或状态。
- 原图上的点击、逐步高亮或联动能定位证据。

如果交互只是展开静态文字、装饰动画或 hover 高亮，改用静态。

## 常用信息图形态

| 内容 | 推荐形态 |
| --- | --- |
| 流程、pipeline | 带方向的步骤流、泳道、阶段门 |
| 因果、机制 | 因果链、反馈回路、输入—处理—输出 |
| 架构、分层 | 水平分层、模块连接、数据流箭头 |
| 人物、角色 | 分组关系网、层级树、关系矩阵 |
| 时间、演进 | 时间轴、阶段带、转折节点 |
| 状态机 | 状态节点、条件标签、迁移箭头 |
| 对比、选型 | 双栏对比、矩阵、二维定位 |
| 分类、构成 | 树状层级、嵌套分区、分组列表 |
| 算法、证明 | 分步画布、当前步骤高亮、前后状态 |
| 参数、物理 | 滑块、实时数值、轨迹或曲线联动 |

不要把“标题 + 若干等大卡片”当成默认信息图。每个节点和连线都应有信息意义。

## 素材形态

- `none`：用 HTML/SVG/Canvas 自绘抽象结构。
- `text_context` / `verified_sources`：只使用已确认事实构图。
- `structured_data`：可用于数值反馈；标准统计图优先 ECharts。
- `user_image + preserve`：将原图作为 `<img>` 底图，叠加 SVG/DOM；不得改变原图。
- `user_image + observe_for_schematic`：提取可直接观察的结构特征，自绘并明确标注“示意”。

## 输出约束

- 用 ` ```html type="renderer" ` 代码块包裹；**禁** `<!DOCTYPE>`/`<head>`/`<body>`/`<TEXT>`。
- 首块先写 `<html style="margin:0;padding:0;">`，紧接一个非空的 HTML `<title>`（如 `<title>二分查找过程演示</title>`），再写透明根 div；命名遵循 `SKILL.md` 的“HTML 网页标题”要求，同一文档只写一个，后续 renderer 片段不重复。
- 不使用 DOCTYPE、head、body、TEXT；CSS 全部内联。
- 根容器自然撑高；禁止 `100vh`、`height:100%` 和根固定大高度。
- 容器使用 `width:100%;box-sizing:border-box`；多列必须弹性换行，长文字不得溢出。
- 正文建议不低于 14px；仅用颜色区分时必须增加文字、形状、线型或图例。
- 优先 HTML/SVG；Canvas 只用于 SVG 难以稳定表达的连续动态绘制。
- 需要脚本时使用 IIFE、try/catch 和 DOM 判空，不监听 `DOMContentLoaded`。
- 地图库和地图组件一律禁止。

## 内容校验

- **图文一致**：正文、图中信息与素材使用同一份已核对的数据和事实，数值、单位、时间与条件一致；多图/关键帧的来源、顺序与说明对应，不用同图冒充不同画面。
- **计算与规则正确**：复算合计、时长、比例与权重；明确区间端点和小数处理，避免重叠与遗漏。源规则冲突时保留歧义并标明待确认。
- **事实与推断分清**：区分事实、已核验结论、假设和未知，保留结论成立条件；未知不按零值处理，箭头与分支须有依据，不把共现当因果。

## 静态信息图要求

- 不操作即可读懂主结论。
- 流程和时间线有明确阅读方向。
- 节点分组、连线含义和图例清晰。
- 图文互补：正文或表格已完整表达时，不再逐项改排成长卡片。图解应让比较、流程、分支或定位更清楚；仅改变配色、卡片或箭头而没有新增理解/操作收益时，保留更清楚的一种表达。用户明确要求图示时，完成有效图形并减少重复正文，不以同义文字卡片替代。
- 复杂关系通过分层、分组或拆图降低交叉。
- 示意结构显式标注“示意”，不暗示精确测量或官方结构。

## 交互要求

- 控件不小于 44×44px，同时支持 click/tap。
- hover 只作桌面增强，不承担核心答案。
- 动画可暂停、可重置，不持续高频空转。
- 操作后提供即时视觉变化和文字或数值反馈。
- 初始静态状态即可理解主结论。
- JavaScript 失败时至少显示文字说明、底图或静态主结构。

## 降级

1. 复杂交互失败 → 静态 HTML/SVG。
2. 外部库失败 → 纯 HTML/SVG/Canvas。
3. 结构或事实不足 → 结构化文字。

不得输出空白 renderer，也不得用不可复核内容替代缺失事实。
